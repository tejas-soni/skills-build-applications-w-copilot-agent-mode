# Required in Codespaces for the public API URL, for example:
# VITE_CODESPACE_NAME=your-codespace-name
#
# Without this value, the frontend safely falls back to http://localhost:8000.
VITE_CODESPACE_NAME=